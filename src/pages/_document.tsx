import { Html, Head, Main, NextScript } from 'next/document';

const Document = () => {
	return (
		<Html lang="ru">
			<Head />
			<body>
				<Main />
				<div id="modals-root" />
				<NextScript />
			</body>
		</Html>
	);
};

export default Document;
